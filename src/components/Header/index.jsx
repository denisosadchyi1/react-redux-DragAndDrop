import React from 'react'
import s from './header.module.scss';

const Header = () => {
  return (
    <header className={s.header}>
      Test Drag and Drop To-Do List
    </header>
  )
}

export default Header
